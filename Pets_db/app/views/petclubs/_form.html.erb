<%= form_for(@petclub) do |f| %>
  <% if @petclub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@petclub.errors.count, "error") %> prohibited this petclub from being saved:</h2>

      <ul>
      <% @petclub.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
